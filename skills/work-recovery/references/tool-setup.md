# Tool Setup

The collector requires local Git and Python 3; it has no package dependencies.

If either command is unavailable, report the missing executable and the attempted command. Do not install software, edit shell configuration, or change the repository. Offer platform-appropriate installation guidance only after the user asks or approves installation.

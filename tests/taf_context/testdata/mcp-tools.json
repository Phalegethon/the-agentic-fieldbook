[
  {
    "annotations": {
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": false,
      "readOnlyHint": true
    },
    "description": "Report native engine availability, index freshness, eligible and excluded path counts, state usage, required authorizations, and next_safe_action for a repository. Read-only apart from an incremental refresh of an already bound index.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "repo": {
          "description": "Absolute path of the Git repository or worktree.",
          "type": "string"
        }
      },
      "required": [
        "repo"
      ],
      "type": "object"
    },
    "name": "inspect",
    "title": "Inspect repository context"
  },
  {
    "_meta": {
      "anthropic/requiresUserInteraction": true
    },
    "annotations": {
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": false,
      "readOnlyHint": false
    },
    "description": "Build a fresh bounded index for the repository in user-local TAF state (never inside the repository). Requires confirm_state_write: true, which you may only pass after the user explicitly approved writing state in this conversation. Query only when the result's next_safe_action is use-index.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "confirm_state_write": {
          "description": "Must be true; the user's explicit state-write authorization.",
          "type": "boolean"
        },
        "repo": {
          "description": "Absolute path of the Git repository or worktree.",
          "type": "string"
        }
      },
      "required": [
        "repo",
        "confirm_state_write"
      ],
      "type": "object"
    },
    "name": "build",
    "title": "Build repository index"
  },
  {
    "annotations": {
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": false,
      "readOnlyHint": true
    },
    "description": "One representative record per file, in path order; narrow with path_prefixes to answer 'what is in directory D'. A query on a bound index behind the working tree refreshes it incrementally first.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "allow_inferred": {
          "default": false,
          "description": "Include findings whose evidence class is inferred.",
          "type": "boolean"
        },
        "languages": {
          "items": {
            "enum": [
              "go",
              "javascript",
              "json",
              "markdown",
              "python",
              "rust",
              "toml",
              "typescript"
            ],
            "type": "string"
          },
          "type": "array"
        },
        "maximum_output_characters": {
          "default": 4000,
          "enum": [
            2000,
            4000,
            8000,
            12000
          ],
          "type": "integer"
        },
        "maximum_results": {
          "default": 8,
          "maximum": 64,
          "minimum": 1,
          "type": "integer"
        },
        "path_prefixes": {
          "description": "Repository-relative path prefixes (case-sensitive).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "repo": {
          "description": "Absolute path of the Git repository or worktree.",
          "type": "string"
        },
        "source_types": {
          "items": {
            "enum": [
              "source",
              "document",
              "configuration"
            ],
            "type": "string"
          },
          "type": "array"
        },
        "symbol_kinds": {
          "items": {
            "enum": [
              "configuration",
              "definition",
              "document-chunk",
              "entry-point",
              "heading",
              "import",
              "module"
            ],
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "repo"
      ],
      "type": "object"
    },
    "name": "repository_map",
    "title": "Repository map"
  },
  {
    "annotations": {
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": false,
      "readOnlyHint": true
    },
    "description": "Find definitions, imports, modules, entry points, and configuration keys by name; every query word must match (camelCase and snake_case split). Use symbol_kinds: [\"definition\"] for 'where is X defined' and [\"import\"] for 'who imports X'. A query on a bound index behind the working tree refreshes it incrementally first.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "allow_inferred": {
          "default": false,
          "description": "Include findings whose evidence class is inferred.",
          "type": "boolean"
        },
        "languages": {
          "items": {
            "enum": [
              "go",
              "javascript",
              "json",
              "markdown",
              "python",
              "rust",
              "toml",
              "typescript"
            ],
            "type": "string"
          },
          "type": "array"
        },
        "maximum_output_characters": {
          "default": 4000,
          "enum": [
            2000,
            4000,
            8000,
            12000
          ],
          "type": "integer"
        },
        "maximum_results": {
          "default": 8,
          "maximum": 64,
          "minimum": 1,
          "type": "integer"
        },
        "path_prefixes": {
          "description": "Repository-relative path prefixes (case-sensitive).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "query": {
          "description": "Exact name or a few words; prefixes match.",
          "type": "string"
        },
        "repo": {
          "description": "Absolute path of the Git repository or worktree.",
          "type": "string"
        },
        "source_types": {
          "items": {
            "enum": [
              "source",
              "document",
              "configuration"
            ],
            "type": "string"
          },
          "type": "array"
        },
        "symbol_kinds": {
          "items": {
            "enum": [
              "configuration",
              "definition",
              "document-chunk",
              "entry-point",
              "heading",
              "import",
              "module"
            ],
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "repo",
        "query"
      ],
      "type": "object"
    },
    "name": "search_symbols",
    "title": "Search symbols"
  },
  {
    "annotations": {
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": false,
      "readOnlyHint": true
    },
    "description": "Find Markdown headings and document chunks; headings rank above body chunks. Use two or three words from the expected heading. A query on a bound index behind the working tree refreshes it incrementally first.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "allow_inferred": {
          "default": false,
          "description": "Include findings whose evidence class is inferred.",
          "type": "boolean"
        },
        "languages": {
          "items": {
            "enum": [
              "go",
              "javascript",
              "json",
              "markdown",
              "python",
              "rust",
              "toml",
              "typescript"
            ],
            "type": "string"
          },
          "type": "array"
        },
        "maximum_output_characters": {
          "default": 4000,
          "enum": [
            2000,
            4000,
            8000,
            12000
          ],
          "type": "integer"
        },
        "maximum_results": {
          "default": 8,
          "maximum": 64,
          "minimum": 1,
          "type": "integer"
        },
        "path_prefixes": {
          "description": "Repository-relative path prefixes (case-sensitive).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "query": {
          "type": "string"
        },
        "repo": {
          "description": "Absolute path of the Git repository or worktree.",
          "type": "string"
        },
        "source_types": {
          "items": {
            "enum": [
              "source",
              "document",
              "configuration"
            ],
            "type": "string"
          },
          "type": "array"
        },
        "symbol_kinds": {
          "items": {
            "enum": [
              "configuration",
              "definition",
              "document-chunk",
              "entry-point",
              "heading",
              "import",
              "module"
            ],
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "repo",
        "query"
      ],
      "type": "object"
    },
    "name": "search_docs",
    "title": "Search documentation"
  },
  {
    "annotations": {
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": false,
      "readOnlyHint": true
    },
    "description": "Fetch the verified source lines for result identities returned by an earlier query; the only tool whose output may be quoted as evidence. If the result is `partial` with `returned_count` 0 and `omitted_count` 1, the snippet did not fit the output budget: call again with a larger `maximum_output_characters` (8000 or 12000). A query on a bound index behind the working tree refreshes it incrementally first.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "allow_inferred": {
          "default": false,
          "description": "Include findings whose evidence class is inferred.",
          "type": "boolean"
        },
        "maximum_output_characters": {
          "default": 4000,
          "enum": [
            2000,
            4000,
            8000,
            12000
          ],
          "type": "integer"
        },
        "maximum_results": {
          "default": 8,
          "maximum": 64,
          "minimum": 1,
          "type": "integer"
        },
        "repo": {
          "description": "Absolute path of the Git repository or worktree.",
          "type": "string"
        },
        "result_ids": {
          "description": "result_identity values from a previous query.",
          "items": {
            "type": "string"
          },
          "minItems": 1,
          "type": "array"
        }
      },
      "required": [
        "repo",
        "result_ids"
      ],
      "type": "object"
    },
    "name": "source_snippets",
    "title": "Source snippets"
  },
  {
    "annotations": {
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": false,
      "readOnlyHint": true
    },
    "description": "Follow the relationships of result identities returned by an earlier query: direction callers answers 'who calls X', callees 'what does X depend on', importers 'who uses module M', and imports 'what does X import'. Every finding carries relation, edge_evidence, reference_line, and reference_count; edge_evidence inferred is a name match, never proof, and is returned only with allow_inferred. A query on a bound index behind the working tree refreshes it incrementally first.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "allow_inferred": {
          "default": false,
          "description": "Include findings whose evidence class is inferred.",
          "type": "boolean"
        },
        "direction": {
          "description": "Which side of the relationship to return.",
          "enum": [
            "callers",
            "callees",
            "importers",
            "imports"
          ],
          "type": "string"
        },
        "languages": {
          "items": {
            "enum": [
              "go",
              "javascript",
              "json",
              "markdown",
              "python",
              "rust",
              "toml",
              "typescript"
            ],
            "type": "string"
          },
          "type": "array"
        },
        "maximum_output_characters": {
          "default": 4000,
          "enum": [
            2000,
            4000,
            8000,
            12000
          ],
          "type": "integer"
        },
        "maximum_results": {
          "default": 8,
          "maximum": 64,
          "minimum": 1,
          "type": "integer"
        },
        "path_prefixes": {
          "description": "Repository-relative path prefixes (case-sensitive).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "repo": {
          "description": "Absolute path of the Git repository or worktree.",
          "type": "string"
        },
        "result_ids": {
          "description": "result_identity values of the anchor symbols or modules.",
          "items": {
            "type": "string"
          },
          "maxItems": 16,
          "minItems": 1,
          "type": "array"
        },
        "source_types": {
          "items": {
            "enum": [
              "source",
              "document",
              "configuration"
            ],
            "type": "string"
          },
          "type": "array"
        },
        "symbol_kinds": {
          "items": {
            "enum": [
              "configuration",
              "definition",
              "document-chunk",
              "entry-point",
              "heading",
              "import",
              "module"
            ],
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "repo",
        "result_ids",
        "direction"
      ],
      "type": "object"
    },
    "name": "related_symbols",
    "title": "Related symbols"
  },
  {
    "annotations": {
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": false,
      "readOnlyHint": true
    },
    "description": "Answer 'what did I change on this branch': the definitions, entry points, and modules whose lines a changed hunk touches, between a base ref and the working tree (committed, staged, unstaged, and untracked changes). The result carries the resolved base; the warning changed-path-not-indexed means the change set reached files the index does not carry. A query on a bound index behind the working tree refreshes it incrementally first.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "allow_inferred": {
          "default": false,
          "description": "Include findings whose evidence class is inferred.",
          "type": "boolean"
        },
        "base": {
          "description": "Git ref or commit the change set is measured against. Defaults to the branch's upstream main, then origin/HEAD, then a local main/master; uncommitted changes are always included.",
          "type": "string"
        },
        "languages": {
          "items": {
            "enum": [
              "go",
              "javascript",
              "json",
              "markdown",
              "python",
              "rust",
              "toml",
              "typescript"
            ],
            "type": "string"
          },
          "type": "array"
        },
        "maximum_output_characters": {
          "default": 4000,
          "enum": [
            2000,
            4000,
            8000,
            12000
          ],
          "type": "integer"
        },
        "maximum_results": {
          "default": 8,
          "maximum": 64,
          "minimum": 1,
          "type": "integer"
        },
        "path_prefixes": {
          "description": "Repository-relative path prefixes (case-sensitive).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "repo": {
          "description": "Absolute path of the Git repository or worktree.",
          "type": "string"
        },
        "source_types": {
          "items": {
            "enum": [
              "source",
              "document",
              "configuration"
            ],
            "type": "string"
          },
          "type": "array"
        },
        "symbol_kinds": {
          "items": {
            "enum": [
              "configuration",
              "definition",
              "document-chunk",
              "entry-point",
              "heading",
              "import",
              "module"
            ],
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "repo"
      ],
      "type": "object"
    },
    "name": "changed_symbols",
    "title": "Changed symbols"
  },
  {
    "annotations": {
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": false,
      "readOnlyHint": true
    },
    "description": "Answer 'what could my change break': the one-hop callers of the changed definitions and the importers of the changed modules and definitions, each candidate attributed to the changed symbols it depends on in `anchors`. Every candidate's edge is the strongest of its anchors; edge_evidence inferred is a name match, never proof, and is returned only with allow_inferred. Read `changed` for the change set itself. A query on a bound index behind the working tree refreshes it incrementally first.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "allow_inferred": {
          "default": false,
          "description": "Include findings whose evidence class is inferred.",
          "type": "boolean"
        },
        "base": {
          "description": "Git ref or commit the change set is measured against. Defaults to the branch's upstream main, then origin/HEAD, then a local main/master; uncommitted changes are always included.",
          "type": "string"
        },
        "maximum_output_characters": {
          "default": 4000,
          "enum": [
            2000,
            4000,
            8000,
            12000
          ],
          "type": "integer"
        },
        "maximum_results": {
          "default": 8,
          "maximum": 64,
          "minimum": 1,
          "type": "integer"
        },
        "repo": {
          "description": "Absolute path of the Git repository or worktree.",
          "type": "string"
        }
      },
      "required": [
        "repo"
      ],
      "type": "object"
    },
    "name": "impact_candidates",
    "title": "Impact candidates"
  },
  {
    "annotations": {
      "destructiveHint": false,
      "idempotentHint": true,
      "openWorldHint": false,
      "readOnlyHint": true
    },
    "description": "Answer 'how is this repository organized, where is the code, where do I start': one row per directory in `groups` with its file, definition, entry-point, document, and configuration counts and its languages, plus a ranked file layer in `findings` that leads with entry points and well-known entry file names. `overview.root` names what was described and `overview.other_group_count` how many directories the row `*` folds together. Start here in an unfamiliar repository, then narrow with path_prefixes to answer 'what is in directory D'; several path_prefixes are not composed, so the first in sorted order becomes the root and the warning overview-root-first-prefix says so. The group table is never trimmed: when the serialized answer is longer than maximum_output_characters the warning output-budget-exceeded says so, and a larger budget or fewer maximum_results is the way to fit it. maximum_output_characters defaults to 8000 here, not the other tools' 4000, because the group table alone is about 3700 characters of canonical JSON, leaving room for the ranked file layer. A query on a bound index behind the working tree refreshes it incrementally first.",
    "inputSchema": {
      "additionalProperties": false,
      "properties": {
        "allow_inferred": {
          "default": false,
          "description": "Include findings whose evidence class is inferred.",
          "type": "boolean"
        },
        "languages": {
          "items": {
            "enum": [
              "go",
              "javascript",
              "json",
              "markdown",
              "python",
              "rust",
              "toml",
              "typescript"
            ],
            "type": "string"
          },
          "type": "array"
        },
        "maximum_output_characters": {
          "default": 8000,
          "enum": [
            2000,
            4000,
            8000,
            12000
          ],
          "type": "integer"
        },
        "maximum_results": {
          "default": 8,
          "maximum": 64,
          "minimum": 1,
          "type": "integer"
        },
        "path_prefixes": {
          "description": "Repository-relative path prefixes (case-sensitive).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "repo": {
          "description": "Absolute path of the Git repository or worktree.",
          "type": "string"
        }
      },
      "required": [
        "repo"
      ],
      "type": "object"
    },
    "name": "repository_overview",
    "title": "Repository overview"
  }
]
